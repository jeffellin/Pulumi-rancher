#@ load("@ytt:data", "data")

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: stack-config
  namespace: continuousdelivery-resources
data:
  cluster_name: #@ data.values.config.cluster_name
  cluster_region: #@ data.values.config.region
  cluster_zone: #@ data.values.config.zone